# Local dev: point at any Postgres instance, e.g. a local server or a free Neon branch.
# In Vercel: added automatically when you attach Vercel Postgres (Storage tab) to the project —
# just make sure the resulting env var is named exactly DATABASE_URL.
DATABASE_URL="postgresql://user:password@host:5432/dbname?sslmode=require"
